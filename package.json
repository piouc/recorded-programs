{
  "ava": {
    "require": [
      "esm"
    ]
  },
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run js -- -d -w & npm run css -- -w -v & watch 'npm run static' ./src/static/ &  http-server dist",
    "build": "rm -rf ./dist/* && npm run static && npm run js -- -p & npm run css -- -u cssnano",
    "static": "rsync -av ./src/static/ ./dist/",
    "js": "webpack --config webpack.config.js",
    "css": "postcss --no-map -u postcss-import -u postcss-nested -u autoprefixer -u postcss-sorting -o dist/index.css src/css/index.css",
    "test": "ava"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.0.0-rc.1",
    "@babel/preset-env": "^7.0.0-rc.1",
    "@babel/register": "^7.0.0-rc.1",
    "autoprefixer": "^9.1.1",
    "ava": "^1.0.0-beta.7",
    "babel-loader": "^8.0.0-beta.4",
    "cssnano": "^4.0.5",
    "esm": "^3.0.76",
    "http-server": "^0.11.1",
    "postcss": "^7.0.2",
    "postcss-cli": "^6.0.0",
    "postcss-import": "^12.0.0",
    "postcss-nested": "^3.0.0",
    "postcss-sorting": "^4.0.0",
    "watch": "^1.0.2",
    "webpack": "^4.16.5",
    "webpack-cli": "^3.1.0"
  }
}
